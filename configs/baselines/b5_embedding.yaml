# B5 — plain embedding classifier on E(c(x)). NO-INVARIANCE FLOOR.
# Detector head only; every representation-shaping term off. CANOPI must beat this.
name: b5_embedding
seeds: [42, 2025, 7, 1337, 314]
split: {scheme: "60/20/20", seed: 1337, stratified: true}
encoder:
  backend: sentence-transformers
  model_name: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
  canonicalize_first: true
  cache: true
model: {head_depth: 2, hidden: 256, out_dim: 128, dropout: 0.1, freeze_encoder: true}
loss:  {lam1_inv: 0.0, lam2_hardneg: 0.0, lam3_drift: 0.0, lam4_pauc: 0.0}
augment: {families: [], crosslingual: [], intent_filter: false}   # no views
train: {epochs: 30, batch_size: 256, lr: 0.001, wd: 0.0001}
eval:  {target_fpr: 0.01, bootstrap_resamples: 1000, bootstrap_seed: 1337}
